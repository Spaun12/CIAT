-- Select specific columns from the CustomerAddresses view
SELECT 
    CustomerID,
    LastName,
    FirstName,
    BillLine1
FROM 
    CustomerAddresses;

-- Verify the result by checking the output for expected data
